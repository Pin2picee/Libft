#include "../minishell.h"
/*
void print_envp(t_minishell *data)
{
    size_t i = 0;
    printf("Variables d'environnement non triées (envp) :\n");
    while (data->envp[i])
    {
        printf("%s\n", data->envp[i]);
        i++;
    }
}

void print_export(t_minishell *data)
{
    size_t i = 0;
    printf("Variables d'environnement triées (export) :\n");
    while (data->export[i])
    {
        printf("declare -x %s\n", data->export[i]);
        i++;
    }
}

void display_env(t_minishell *data)
{
    print_envp(data);
    printf("\n");
    print_export(data);
}
*/
