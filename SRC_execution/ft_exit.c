#include "../minishell.h"
/*

void    ft_exit()
{
    if (!tab[1])
    {
        exit(0);
    }
    si il y a qu un seul arg, exit_code = 0;
    faire % 256 du nombre;
    gestion d erreurs:
    {
        qd ya plus de 2 arg;
        quand l argument n est pas un int; -> bash: exit: n: numeric argument required
    }
    printf("")
    exit(ft_itoa(av[1]));
}
*/
